program Dangledif ;
var x: boolean;
label john;
forward function ok(var k : real):real;
function ok(l : real ):real; 
  begin end;
(*forward procedure ok();*)
begin
(* * 
 *)
comparedo := true ; 
if comparedo then
 if not comparedo then
  mitsos := 42
 else
  john := mitsos
end.
