program Dangledif ;
var x: boolean;
label john;
forward function okk(var okm, okk, okl:boolean): real;
procedure ok(); 
  begin end;
begin
(* * 
 *)
comparedo := true ; 
if comparedo then
 if not comparedo then
  mitsos := 42
 else
  john := mitsos
end.
